#include "<%= config[:underscored_name] %>.h"

static VALUE m<%= config[:constant_array].join %>;

void
Init_<%= config[:underscored_name] %>(void)
{
  m<%= config[:constant_array].join %> = rb_define_module(<%= config[:constant_name].inspect %>);
}
